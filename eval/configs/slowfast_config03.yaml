# LLaDA SlowFast 默认配置
# 使用方法: bash eval/scripts/evaluate_slowfast_flexible.sh eval/configs/slowfast_default_config.yaml

# 注意力模式配置
attention_mode: slowfast

# SlowFast 参数配置
slowfast_dominant_tokens: 192
slowfast_pooled_tokens: 0
slowfast_full_layer_interval: 4

# 模型路径
base_model_path: /mnt/yrfs/bzh/code/LLaDA-V/LLaDA-V-8B

# 评估任务列表（用逗号分隔）
task_names: mmmu_val,mmmu_pro_standard,mmstar,ai2d,seedbench,mmbench_en_dev,mmmu_pro_vision,muirbench,videomme,mlvu_dev,mme,realworldqa,chartqa,docvqa_val,infovqa_val

# 输出路径基础目录
output_base: exp/eval_slowfast_flexible_config03

# GPU 配置
total_gpus: 8
batch_size: 1
num_processes: 1
